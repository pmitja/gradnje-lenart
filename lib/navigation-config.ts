/**
 * Navigation configuration
 * Contains settings for navigation behavior across different routes
 */

/**
 * List of paths that need a dark background in the navigation when not scrolled
 * Add paths here to apply a dark background overlay to the navigation
 */
export const PATHS_WITH_DARK_NAV = [
  '/politika-zasebnosti',
  '/auth/login',
  '/auth/register',
  // Add more paths as needed
] 